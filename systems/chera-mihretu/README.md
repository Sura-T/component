# Chera Mihretu Systems

This folder contains individual assembly-only systems that compose features from workspace packages.

- `system-a`: Sales snapshot
- `system-b`: Incident trend viewer

Implementation rule: systems should orchestrate package exports, not duplicate package internals.
